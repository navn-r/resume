language: node_js
node_js:
- node
install:
- npm i
script:
- npm run build:pdf
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: docs
  target_branch: master
  keep_history: true
  on:
    branch: develop
